@model GeekCoding.Data.Models.Announcement

@{
    ViewData["Title"] = "DeleteAnnouncement";
}

<h2>DeleteAnnouncement</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Announcement</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AnnouncementContent)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AnnouncementContent)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DateAdded)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DateAdded)
        </dd>
    </dl>

    <form asp-action="DeleteAnnouncement">
        <input type="hidden" asp-for="AnnouncementId" />
        <input type="hidden" asp-for="ContestId" />
        <input type="hidden" asp-for="AnnouncementContent"/>
        <input type="submit" value="Delete" class="btn btn-default" />
    </form>
</div>

<div>
    <a asp-action="Announcement" asp-route-Id="@Model.ContestId"> <i class="glyphicon glyphicon-arrow-left"></i> &nbsp; Back to announcement list</a>
</div>
